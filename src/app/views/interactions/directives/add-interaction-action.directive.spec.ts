import { AddInteractionActionDirective } from './add-interaction-action.directive';

describe('AddInteractionActionDirective', () => {
  it('should create an instance', () => {
    const directive = new AddInteractionActionDirective();
    expect(directive).toBeTruthy();
  });
});
